# Union of operator + corpus dependencies.
# Operator side: structured-data simulation
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.0
scikit-learn>=1.5.0
plotly>=5.24.0
streamlit>=1.40.0
pyarrow>=15.0.0
requests>=2.31.0

# Corpus side: RAG over PDF documents
llama-index>=0.12.0
llama-index-readers-file>=0.5.0
llama-index-vector-stores-chroma>=0.4.0
llama-index-embeddings-huggingface>=0.5.0
llama-index-llms-anthropic>=0.6.0
sentence-transformers>=3.0.0
chromadb>=0.5.0
pypdf>=5.1.0
pdfplumber>=0.11.0

# Shared
anthropic>=0.40.0
python-dotenv>=1.0.0

# Force a protobuf version compatible with chromadb's generated _pb2.py files.
# Without this pin, Streamlit Cloud was resolving an older protobuf (< 5.x) that
# raised "Descriptors cannot be created directly" on chromadb import.
protobuf>=5.0.0
